[project]
name = "diderot-ml-specialization"
version = "0.1.0"
description = "Reproducible companion code for the Diderot ML book"
requires-python = ">=3.10"
dependencies = ["numpy>=1.26", "matplotlib>=3.8"]
